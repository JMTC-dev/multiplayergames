# Rivet Configuration
# Rivet runs on the same domain as Next.js, so no separate host configuration is needed.
# WebSocket connections are automatically made to /api/rivet/unoGameActor/{roomId}
# No environment variables required for local development or production.
